import { extendTheme } from "@chakra-ui/react";

const colorPalette = {
    backGround: 'blue.100',
    foreGround: 'blue.400',
    accent: 'blue.700'
}

const theme = extendTheme({colorPalette})


export default theme